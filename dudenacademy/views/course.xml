<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- dudenacademy.course tree view -->
    <record id="dudenacademy_course_view_tree" model="ir.ui.view">
        <field name="name">dudenacademy.course.view.tree</field>
        <field name="model">dudenacademy.course</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="user_id"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <!-- dudenacademy.course form view -->
    <record id="dudenacademy_academy_view_form" model="ir.ui.view">
        <field name="name">dudenacademy.course.view.form</field>
        <field name="model">dudenacademy.course</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="user_id"/>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description"/>
                        </page>
                        <page string="Sessions" name="session_ids">
                            <field name="session_ids"/>
                        </page>

                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- odooacademy.course search view -->
    <record id="dudenacademy_course_view_search" model="ir.ui.view">
        <field name="name">dudenacademy_course.view.search</field>
        <field name="model">dudenacademy.course</field>
        <field name="arch" type="xml">
            <search string="Search Description">
                <!-- Add your fields here -->
                <field name="name"/>
                <field name="description"/>
            </search>
        </field>
    </record>

    <!-- model.name action window -->
    <record id="course_action" model="ir.actions.act_window">
        <field name="name">Course</field>
        <field name="res_model">dudenacademy.course</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- This Menu Item will appear in the Upper bar, that's why It needs NO parent or action -->
    <menuitem id="dudenacademy_menu_root" name="Duden Academy" sequence="10"/>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="course_menu_act" name="Course" parent="dudenacademy_menu_root" action="course_action" sequence="10"/>

</odoo>
